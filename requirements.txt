python-telegram-bot==20.6
pymongo==4.6.1
dnspython>=2.4.2

# For environment variables handling (optional but recommended)
python-dotenv==1.0.1

# For async-compatible HTTP requests (used internally by telegram bot lib)
httpx==0.27.0

# If you're working with image files (welcome image)
Pillow==10.2.0